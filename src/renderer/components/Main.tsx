import { Typography, Box } from '@mui/material';

const Main = () => {
    return (
        <Box>
            <Typography variant="h5">Dataset-JSON Viewer</Typography>
        </Box>
    );
};

export default Main;
